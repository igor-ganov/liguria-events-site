/** The basemap's required credit line, shown by the map's attribution control
 *  and repeated on the vector source so a style reload never drops it. */
export const MAP_ATTRIBUTION = '© OpenMapTiles © OpenStreetMap contributors';
