/** The picture every archived page wears. Shipped by us, drawn once
 *  (scripts/build-archive-cover.ts), and the same on every one of them: a
 *  reader who has seen it twice knows what the page is before reading a word. */
export const ARCHIVE_COVER = '/og/archive.png';
