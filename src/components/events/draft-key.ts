/** Where a half-written submission waits while its author signs in. Session
 *  storage, not local: a draft is worth surviving a redirect, not a week. */
export const DRAFT_KEY = 'dovego:event-draft';
