/** The columns every event read selects, so the row shape is the same wherever
 *  it is decoded. */
export const EVENT_COLUMNS =
  'id, title_en, title_it, title_ru, desc_en, desc_it, desc_ru, start_date, end_date, ' +
  'categories, venue, lat, lng, cover_image, free, gem, sessions, kind';
