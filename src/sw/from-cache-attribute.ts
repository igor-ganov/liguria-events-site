/** Written on the document element of a page served out of storage, carrying
 *  the moment it was stored. The page reads it before it paints. */
export const FROM_CACHE_ATTRIBUTE = 'data-from-cache';
